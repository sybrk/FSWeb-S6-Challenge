import React from "react";

const Species = (props) => {
    
    const {species} = props;

    const {name} = species;

    return (
        <>
            <h1>{name}</h1>
            
            
        </>
    )
}

export default Species;